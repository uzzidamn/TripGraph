# Bucket 2 — Decisions & Assumptions Log
Generated by: Claude Sonnet 4.6 on 2026-06-17

---

## Pre-Specified Decisions Applied

| # | Decision | Value Applied |
|---|----------|---------------|
| 1 | Default group_size | 4 — applied in chat_parser defaults |
| 2 | Default hotel_tier | "comfort" — applied in chat_parser defaults |
| 3 | Default risk_tolerance | "medium" — applied in chat_parser defaults |
| 4 | Default origin | "Gurugram" — applied in chat_parser defaults |
| 5 | Default trip_duration | "2D1N" — applied in chat_parser defaults |
| 6 | JSON parsing from LLM | _parse_llm_json() strips ```json and ``` fences before json.loads() |
| 7 | Invalid LLM JSON | Retry once; on second failure log and use empty defaults. Does not crash. |
| 8 | LLM call failure | Catches Exception, logs with print(f"❌ LLM call failed: {e}"), then re-raises |
| 9 | LLM temperature | 0 — set in get_llm() from LLM_TEMPERATURE env var defaulting to "0" |
| 10 | Rate limit handling | Not pre-emptively added; can be added with time.sleep(4) if hit during testing |
| 11 | Empty must_include | data_retriever passes None to get_activities() — returns all activities |
| 12 | avoid_night_driving=false | Not penalized — scorer only applies penalty when True AND night_allowed |
| 13 | Number of alternatives | Up to 3 (sorted_candidates[1:4]) |
| 14 | No valid candidates | Best invalid candidate selected as selected_itinerary; is_valid=False in report |
| 15 | Map points format | {lat, lng, label, type} — types: origin/waypoint/destination/hotel/activity |
| 16 | Map point types | "origin", "waypoint", "destination", "hotel", "activity" |
| 17 | Public function names | run_workflow(chat_messages) and run_replan_workflow(state, delay_event) |
| 18 | LLM provider packages | langchain-google-genai for Gemini (default); others via lazy imports |
| 19 | Mock vs real tool imports | try/except Exception around each tool call at runtime (not ImportError at import) |
| 20 | Required fields to proceed | origin AND (budget OR trip_duration) AND one preference field |

---

## Unspecified Decisions Made During Build

### `_parse_llm_json` defined in `chat_parser.py` and imported by `constraint_validator.py`
The spec said each node should handle JSON parsing but didn't specify where the helper lives.
Placed in chat_parser.py and re-imported by constraint_validator.py to avoid duplication.

### Tool fallback is try/except Exception (not try/except ImportError)
The spec says "try real imports first, fall back to mock if ImportError." However, all tool
modules exist (Bucket 5 implemented them) — they fail at runtime if Neo4j is offline, not at
import time. Used `try/except Exception` around each tool call instead of around the import.

### `destination` field injected onto hotel/activity dicts after tool call
The Neo4j queries via `queries.py` don't return a `destination` field on hotel/activity nodes
(it's expressed as a graph edge, not a property). The `candidate_generator` filters hotels by
`h.get("destination") == destination`. Added `h.setdefault("destination", dest)` in the
data_retriever to ensure the filter works correctly with real Neo4j results.

### `route_id` field injected onto transport/waypoint dicts
Same reason as above — added `t.setdefault("route_id", route_id)` and similar for waypoints.

### Mock data in `data_retriever.py` only covers Rishikesh and Jaipur
The mock data in the spec (Section B.4) only provided Rishikesh examples. Extended to include
Jaipur mocks as well so the pipeline runs meaningfully when a heritage destination is requested.

### Prompt kept under ~400 tokens for explainer
Passed a summarised itinerary dict to the explainer rather than the raw itinerary. The raw dict
includes `trip_graph` (non-serializable) and is too long for the free-tier context. Summary
includes: destination, transport mode/tier, hotel name/price, total cost, activities list.

### `_main_app` and `_replan_app` compiled at module load time
The spec didn't specify when to compile. Compiled once at import time so first `run_workflow()`
call isn't slow. Each call to `run_workflow()` creates a fresh `initialize_state()`.

---

## Deviations from Spec

### `trip_graph` explicitly popped from `selected_itinerary` before storing in state
The spec mentions in pitfalls: "The trip_graph field contains dataclass instances — use
default=str in any json.dumps() call." Took the stricter approach: remove it from the
selected itinerary stored in state entirely, since it is never needed downstream (API layer
and frontend never use it). This avoids serialization issues at the FastAPI boundary.

---

---

## v2 Decisions (bucket2_v2.md)

### Pre-Specified Decisions Applied

| # | Decision | Value Applied |
|---|----------|---------------|
| 21 | Memory unavailable | Continue with empty memory — `get_user_memory()` returns `{}` on missing user_id or unknown id |
| 22 | Memory priority | Explicit user input > current chat > memory — memory only fills absent fields |
| 23 | Memory update timing | End of successful workflow — `memory_updater_node` runs in fan-out after planner |
| 24 | Parallel retrieval workers | 5 — `ThreadPoolExecutor(max_workers=5)` in `data_retriever_node` |
| 25 | Parallel agent execution | Enabled — `explainer` and `memory_updater` fan-out concurrently after `planner_orchestrator` |
| 26 | Memory scope | user (individual) — keyed by `user_id`, not group chat ID |
| 27 | Past-trip deduplication | Enabled by default — routes filtered by `visited_destinations` in both data_retriever and planner |
| 28 | Dedup override keyword | `again`, `same place`, `revisit`, `back to`, `once more` — detected in `memory_agent_node` |
| 29 | Dedup feedback message | "Since you've already travelled to {destination}, we're skipping that..." — injected via `memory_context.skip_reason` into EXPLAINER_SYSTEM |
| 30 | Guardrail: non-trip message | Silently ignore — `action=ignore`, `response=None`, pipeline exits |
| 31 | Guardrail: invalid prompt | `action=clarify`, clarification message returned |
| 32 | Guardrail: similar trip, completed | `action=confirm`, asks if user wants new plan |
| 33 | Guardrail: similar trip, planned | `action=confirm`, asks if trip is still on |
| 34 | Guardrail: similar trip, cancelled | `action=proceed`, cancellation note in `response`, Chat Parser runs |
| 35 | Guardrail confidence threshold | < 0.4 → classified as non_trip regardless of `intent` field |
| 36 | New files | `backend/agents/nodes/guardrail.py` |

### Unspecified v2 Decisions Made During Build

**Memory store: in-process dict (thread-safe)**
Used `threading.Lock` around `_STORE` operations. No persistence across process restarts. Interface (`get_user_memory`, `update_user_memory`, `clear_user_memory`) can be swapped for a DB-backed implementation without changing any node code.

**Memory scope rename: group_chat_id → user_id**
Spec was updated mid-build: `group_chat_id`/`group_profile`/`get_group_memory`/`update_group_memory` renamed to `user_id`/`user_profile`/`get_user_memory`/`update_user_memory`. Memory is now individual-user-scoped, not group-chat-scoped.

**Guardrail: pure Python similar-trip detection**
The similar-trip check (section 0.3) is pure Python string matching (destination name in chat text). No LLM call for this step — the LLM is used only for intent classification (trip/non_trip/invalid). This avoids a second LLM call and is deterministic enough for the supported cases.

**Guardrail: fallback to proceed on LLM error**
If the LLM classification call fails for any reason, `guardrail_node` returns `action=proceed` with `reason=guardrail_error` rather than blocking the pipeline. The LLM error is logged to stdout.

**Parallel retrieval: ThreadPoolExecutor within data_retriever_node, not LangGraph Send API**
The spec describes 5 parallel sub-nodes; implemented as 5 concurrent `ThreadPoolExecutor` futures within a single LangGraph node. This is functionally equivalent and simpler than the LangGraph Send API, which would require restructuring the graph into a subgraph. Upgrade to Send API when real async tools are introduced.

**Dedup applied at both data_retriever and planner_orchestrator**
The spec says data_retriever filters visited destinations. Added a secondary guard in `planner_orchestrator_node` to catch cases where the node is called directly (e.g., in tests) without going through the retriever. The planner also sets `memory_context["all_candidates_visited"] = True` when all routes are filtered.

**`trace_id` removed from TripState**
The v2 TripState spec does not include `trace_id`. Removed from the state class and `init_state()`. The `get_trace_url()` call in `workflow.py` was removed accordingly. LangSmith tracing still works via env vars (`LANGCHAIN_TRACING_V2`, `LANGCHAIN_API_KEY`) — just not stored in state.

**Replan workflow now includes memory_updater**
The replan graph is: `replanner_agent → memory_updater → END`. The memory_updater persists the replanned itinerary as a new `past_trips` entry with `status=planned`. This ensures memory is updated after both initial planning and replanning.

---

## External Assumptions

- `langgraph`, `langchain-core`, `langchain-google-genai` are in `requirements.txt` (Bucket 5)
- `PYTHONPATH=.` must be set when running from TripGraph root
- `GOOGLE_API_KEY` must be set in `.env` for LLM tests to run
- Neo4j is optional — pipeline falls back to mock data gracefully when offline
- `backend/planner/` and `backend/tools/` modules from Bucket 5 are present and importable

---

## Validation Results

```
============================================================
Bucket 2 — Agentic Pipeline Validation
============================================================

📋 Test 1: TripState schema
  ✅ TripState imported successfully
  ✅ initialize_state() has all 24 required fields
  ✅ raw_chat correctly initialized
  ✅ is_ready_to_plan defaults to False
  ✅ selected_itinerary defaults to None

📝 Test 2: Prompts
  ✅ CHAT_PARSER_SYSTEM loaded (1089 chars)
  ✅ CHAT_PARSER_HUMAN loaded (97 chars)
  ✅ CONSTRAINT_VALIDATOR_SYSTEM loaded (1034 chars)
  ✅ CONSTRAINT_VALIDATOR_HUMAN loaded (89 chars)
  ✅ EXPLAINER_SYSTEM loaded (542 chars)
  ✅ EXPLAINER_HUMAN loaded (395 chars)
  ✅ REPLANNER_EXPLAIN_SYSTEM loaded (451 chars)
  ✅ REPLANNER_EXPLAIN_HUMAN loaded (287 chars)
  ✅ CHAT_PARSER_HUMAN has {chat_messages} placeholder
  ✅ CONSTRAINT_VALIDATOR_HUMAN has {constraints} placeholder

🔄 Test 3: Workflow structure
  ✅ run_workflow imported successfully
  ✅ run_replan_workflow imported successfully
  ✅ run_workflow is callable
  ✅ run_replan_workflow is callable

🤖 Test 4: LLM client
  (requires GOOGLE_API_KEY)

🚀 Test 5: End-to-end workflow
  (requires GOOGLE_API_KEY)
```
